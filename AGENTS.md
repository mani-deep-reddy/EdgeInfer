# EdgeInfer

## Workflow

- Uses **OpenSpec** (`spec-driven` schema) for change management.
- Skills installed in `.opencode/skills/` (openspec-propose, openspec-explore, openspec-apply-change, openspec-archive-change).
- Custom commands in `.opencode/commands/` prefixed with `opsx-`.
- Config: `openspec/config.yaml` (currently default — no project context set).

## Current state

- No application code exists yet.
- No build, test, lint, or typecheck commands configured.
- Not initialized as a git repository.

## When application code is added

- Add developer commands, test shortcuts, and architecture notes here.
- Update `openspec/config.yaml` with tech stack and conventions for AI context.
